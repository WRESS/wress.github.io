'use client'
import { createTheme } from "@mui/material";

export const WRESSTheme = createTheme(
    {
        palette: {
          mode: 'light',
          primary: {
            main: '#9b2121',
          },
          secondary: {
            main: '#9c27b0',
          },
        },
      }
);